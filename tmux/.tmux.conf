###################################################################################
# Terminal & Prefix
###################################################################################
set -g default-shell /bin/zsh
set -g default-terminal "xterm-256color"
set -g terminal-overrides "xterm-256color:RGB"
set -g mouse off
set -s set-clipboard off

# Use Ctrl + a as prefix instead of Ctrl + b
set -g prefix C-a
bind C-a send-prefix
unbind C-b
bind m set -g mouse \; display-message "tmux mouse #{?mouse,on,off}"

# Reload config with prefix + r
unbind r
bind r source-file ~/.tmux.conf\; display "Reloaded!"

bind s choose-tree -sZ
bind w choose-tree -Zw

# Split panes and window behavior
bind-key '%' split-window -h -p 40
set -g base-index 1
set -g renumber-windows on
set -g focus-events off
# Give tmux enough time to recognize multi-byte escape sequences such as
# bracketed paste terminators before forwarding stray characters to zsh.
set -sg escape-time 10
set -g status-right ""

###################################################################################
# Theme: Catppuccin (Dynamic)
###################################################################################
# Catppuccin flavor options: latte, frappe, macchiato, mocha
# Read theme from ~/.config/theme file, default to mocha
run-shell 'tmux set -g @catppuccin_flavor "$(cat ~/.config/theme 2>/dev/null || echo mocha)"'

# Window styling
set -g @catppuccin_window_status_style "rounded"
set -g @catppuccin_window_text "#W"
set -g @catppuccin_window_current_text "#W"

# Status bar customization
set -g status-left "#{E:@catppuccin_status_session}"
set -g status-left-length 30
set -g status-right-length 30
set -g status-justify centre
set -g status-position top
set -g window-status-separator " "

###################################################################################
# Plugins (via TPM)
###################################################################################
# Plugin manager
set -g @plugin 'tmux-plugins/tpm'

# Navigation integration with Vim
set -g @plugin 'christoomey/vim-tmux-navigator'

# Catppuccin theme
set -g @plugin 'catppuccin/tmux'

# Initialize TPM (keep this line at the very bottom)
run '~/.tmux/plugins/tpm/tpm'
